<rule>
condition = var(value) eq "lVSi"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lOFC"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rOFC"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rMPFC"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lamy"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rantCing"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lantCing"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lantIns"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rpSTS"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lpSTS"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rTPJ"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rmSTS"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rFFA"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lFFA"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rFusG"
label_font = bold
</rule>

<rule>
condition = var(value) eq "lFusG"
label_font = bold
</rule>

<rule>
condition = var(value) eq "rEBA"
label_font = bold
</rule>